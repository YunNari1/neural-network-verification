name: assign4
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch>=2.0
  - torchvision
  - pytorch-cuda=11.8   # remove if no GPU
  - pip
  - pip:
    - onnx>=1.14
    - onnxruntime>=1.16
    - numpy>=1.24
    - torchvision
    - pillow
    - pyyaml
    - tqdm
